# roll-the-dice
A simple Python program that simulates rolling a dice. The user can roll the dice multiple times, generating random outcomes each time. This project demonstrates the use of random number generation and loops
# Dice Roller (Python)

A simple Python program that simulates rolling a dice and generates a random number each time.

## Features
- Random dice roll
- User can roll multiple times
- Beginner-friendly code

## How to Run
1. Download the file
2. Run:
   python roll.py

## Example
You rolled: 4
